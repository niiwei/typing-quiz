# 新功能说明 v1.2

## 🎉 本次更新内容

### 1. 数据管理增强 - 导入导出功能 ✅

#### 功能说明
支持将测验数据导出为JSON格式,也可以从JSON文件导入测验数据,方便数据备份和迁移。

#### 使用方法

**导出单个测验:**
1. 访问测验管理页面 (http://localhost:8080/manage.html)
2. 在测验列表中找到要导出的测验
3. 点击"导出"按钮
4. 浏览器会自动下载JSON文件

**导出所有测验:**
1. 访问测验管理页面
2. 点击页面顶部的"导出所有测验"按钮
3. 浏览器会下载包含所有测验的JSON文件

**导入测验:**
1. 访问测验管理页面
2. 点击页面顶部的"导入测验"按钮
3. 选择JSON文件(可以是单个测验或多个测验)
4. 系统会显示导入结果(成功/失败数量)

#### JSON格式示例

**单个测验:**
```json
{
  "title": "世界首都",
  "description": "说出世界各国的首都",
  "timeLimit": 600,
  "answers": ["北京", "东京", "伦敦", "巴黎", "华盛顿"]
}
```

**多个测验:**
```json
[
  {
    "title": "测验1",
    "description": "描述1",
    "timeLimit": 300,
    "answers": ["答案1", "答案2"]
  },
  {
    "title": "测验2",
    "description": "描述2",
    "timeLimit": null,
    "answers": ["答案3", "答案4"]
  }
]
```

#### API端点
- `GET /api/import-export/quiz/{id}/export` - 导出单个测验
- `GET /api/import-export/quizzes/export` - 导出所有测验
- `POST /api/import-export/quiz/import` - 导入单个测验
- `POST /api/import-export/quizzes/import` - 批量导入测验

---

### 2. 测验放弃功能 ✅

#### 功能说明
在测验过程中,如果觉得太难或想查看答案,可以点击"放弃"按钮。放弃后:
- 所有未答出的答案会在原位置以**红色**显示
- 已答出的答案保持绿色显示
- 显示最终结果统计

#### 使用方法
1. 开始任意测验
2. 在答题过程中,点击输入框旁边的红色"放弃"按钮
3. 确认放弃
4. 查看所有答案(未答出的用红色标注)

#### 界面变化
- 输入框旁边新增红色"放弃"按钮
- 放弃后,答案网格中:
  - ✅ 绿色 = 已答出的答案
  - ❌ 红色 = 未答出的答案
- 结果统计中会显示"(已放弃)"标记

---

### 3. 数据库管理实时搜索 ✅

#### 功能说明
数据库管理页面现在支持实时搜索,无需点击搜索按钮:
- 输入时自动搜索(300ms防抖)
- 显示所有测验/答案的初始列表
- 搜索结果随输入实时更新
- 清空输入框自动恢复完整列表

#### 使用方法

**按测验检索:**
1. 访问数据库管理页面 (http://localhost:8080/database.html)
2. 在"按测验检索"标签页
3. 页面加载时自动显示所有测验列表
4. 在搜索框中输入:
   - 纯数字 → 按测验ID搜索
   - 文字 → 按测验名称模糊搜索
5. 搜索结果实时更新
6. 清空输入框恢复完整列表

**按答案检索:**
1. 切换到"按答案检索"标签页
2. 页面加载时自动显示所有答案列表
3. 在搜索框中输入:
   - 纯数字 → 按答案ID搜索
   - 文字 → 按答案内容模糊搜索
4. 搜索结果实时更新
5. 清空输入框恢复完整列表

#### 改进点
- ❌ 移除了"搜索"按钮
- ✅ 支持实时搜索(输入即搜索)
- ✅ 初始显示完整列表
- ✅ 300ms防抖优化性能
- ✅ 空输入自动恢复列表

---

## 📊 功能对比

| 功能 | 之前 | 现在 |
|------|------|------|
| 数据导出 | ❌ 不支持 | ✅ 支持JSON导出 |
| 数据导入 | ❌ 不支持 | ✅ 支持JSON导入 |
| 测验放弃 | ❌ 只能等时间到 | ✅ 可随时放弃查看答案 |
| 数据库搜索 | 需要点击按钮 | ✅ 实时搜索 |
| 初始列表 | ❌ 需要手动搜索 | ✅ 自动显示 |

---

## 🎯 使用场景

### 场景1: 备份测验数据
1. 访问测验管理页面
2. 点击"导出所有测验"
3. 保存JSON文件作为备份

### 场景2: 分享测验
1. 导出单个测验为JSON
2. 发送给朋友
3. 朋友导入JSON文件即可使用

### 场景3: 批量创建测验
1. 准备JSON文件(可以包含多个测验)
2. 点击"导入测验"
3. 选择JSON文件
4. 批量创建完成

### 场景4: 学习模式
1. 开始测验
2. 尝试回答
3. 遇到困难时点击"放弃"
4. 查看所有答案学习

### 场景5: 快速查找数据
1. 访问数据库管理页面
2. 直接在搜索框输入关键字
3. 实时查看搜索结果
4. 无需点击按钮

---

## 🔧 技术实现

### 导入导出
- 后端: 新增 `ImportExportController`
- 格式: 标准JSON格式
- 支持: 单个/批量导入导出

### 放弃功能
- 前端: 修改 `QuizController` 和 `UIRenderer`
- 样式: 新增 `.missed` 类(红色显示)
- 逻辑: 放弃时显示所有答案

### 实时搜索
- 前端: 修改 `database.js`
- 优化: 300ms防抖避免频繁请求
- 体验: 输入即搜索,无需按钮

---

## 📝 注意事项

1. **导入数据格式**: 确保JSON格式正确,必须包含 `title` 和 `answers` 字段
2. **放弃操作**: 放弃后无法继续答题,只能重新开始
3. **实时搜索**: 搜索有300ms延迟,避免输入时频繁请求
4. **数据备份**: 建议定期导出数据作为备份

---

**更新日期:** 2025-01-10
**版本:** v1.2
